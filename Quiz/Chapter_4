Q1 Which Python keyword indicates the start of a function definition?
A1 def

Q2 In Python, how do you indicate the end of the block of code that makes up the function?
A2 You de-indent a line of code to the same indent level as the def keyword

Q3 In Python what is the input() feature best described as?
A3 A built-in function

Q4 What does the following code print out? 
def thing(): 
    print('Hello'); 

print('There')
A4 There

Q5 In the following Python code, which of the following is an "argument" to a function? 
x = 'banana'; 
y = max(x); 
print(y)
A5 x

Q6 What will the following Python code print out? 
def func(x) : 
    print(x); 
    
func(10); 
func(20)
A6 10, 20

Q7 Which line of the following Python program will never execute? 
def stuff(): 
    print('Hello') 
    return 
    print('World') 
    
 stuff()
A7 print('World')

Q8 What will the following Python program print out? 
def greet(lang): 
    if lang == 'es': 
        return 'Hola' 
    elif lang == 'fr': 
        return 'Bonjour' 
    else: 
        return 'Hello' 
       
 print(greet('fr'),'Michael')
A8 Bonjour Michael

Q9 What does the following Python code print out? 
(Note that this is a bit of a trick question and the code has what many would consider to be a flaw/bug - so read carefully). 
def addtwo(a, b): 
    added = a + b 
    return a; 
    
 x = addtwo(2, 7); 
 print(x)
 A9 2
 
 Q10 What is the most important benefit of writing your own functions?
 A10 Avoiding writing the same non-trivial code more than once in your program
 
